# Copy this file to .env for local development, or set this as an
# environment variable in your deployment platform (e.g. Vercel) for
# production. Never hardcode this URL directly in source code.
#
# Local development (default if unset):
VITE_API_BASE_URL=http://localhost:8000/api

# Production example (replace with your actual deployed backend URL):
# VITE_API_BASE_URL=https://your-backend.onrender.com/api
